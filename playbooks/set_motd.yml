---
- name: Set MOTD on Arista Switch
  hosts: LEAFS
  gather_facts: no
  tasks:
  - name: Update MOTD
    arista.eos.eos_banner:
      banner: login
      text: "{{ motd_banner }}"
      state: present
